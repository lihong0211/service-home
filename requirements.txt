# Web 框架（由 Flask 迁移至 FastAPI）
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart>=0.0.9
PyMySQL==1.1.0
SQLAlchemy==2.0.23
python-dotenv==1.0.0

# AI
# 向量检索（embedding 模块）
numpy>=2.2.6
faiss-cpu>=1.7.0
qdrant-client>=1.12.0
elasticsearch>=8.0.0
PyPDF2>=3.0.0
pycryptodome>=3.19.0
python-docx>=1.0.0
python-pptx>=0.6.0
# Excel 解析（.xlsx 用 openpyxl，.xls 用 xlrd）
openpyxl>=3.1.0
xlrd>=2.0.0
# 免费 OCR（知识库图片识别，需本机安装 Tesseract：Mac brew install tesseract tesseract-lang）
pytesseract>=0.3.10
Pillow>=10.0.0
faster-whisper>=1.0.0
# WebSocket 使用 FastAPI 内置，无需 flask-sock
edge-tts>=6.1.0
# 文生图（Stable Diffusion XL，diffusers）
torch>=2.0
torchvision
diffusers>=0.30.0
transformers>=4.46.0
accelerate
qwen-vl-utils
av
dashscope>=1.25.0
qwen-agent>=0.0.33
soundfile>=0.12.0
# LangChain SQL Agent（Text2SQL，无需手写提示词）
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
# LangGraph 图工作流（循环、并行、条件路由、人机协作等）
langgraph>=0.2.0
# LangGraph HITL 图的 checkpointer：SQLite 文件跨 worker 进程共享中断/恢复状态
langgraph-checkpoint-sqlite>=2.0.0
# RAGAS：RAG 效果评测（faithfulness/answer_relevancy/context_precision/context_recall/
# answer_correctness），evaluator LLM/embeddings 走 DashScope（OpenAI 兼容），不需要 OpenAI key。
# 锁死版本号：v0.4 相比 v0.3 API 变动很大（collections 模块 + ascore 签名），不锁版本升级会直接炸
ragas==0.4.3
# ragas.testset（测试集自动生成）的间接依赖，不会被 pip 自动装上，需要显式声明
rapidfuzz>=3.0.0
# 股票行情数据（A股实时/历史）
akshare>=1.12.0
# RSS 新闻解析
feedparser>=6.0.0
python-dateutil==2.8.2
mcp>=1.26.0
modelscope>=1.30.0

# === 医疗微调 (Qwen2.5-7B) 必要依赖 ===
bitsandbytes>=0.45.4    # 4bit 量化加载 (load_in_4bit=True)
datasets>=3.1.0
pandas>=2.0.3
peft>=0.15.0            # LoRA / hotswap，unsloth 也依赖
torch>=2.5.1
transformers>=4.51.3
trl>=0.15.2             # SFTTrainer
unsloth>=2025.3.19

# === 可选：非本脚本必需 ===
torchvision>=0.20.1   # 仅视觉模型 (如 Qwen-VL) 需要，纯文本 7B 可删
# unsloth_zoo>=2025.3.17  # unsloth 预设/示例，可删
# vllm>=0.7.0            # 推理加速/服务，做训练可删
# wandb>=0.19.11         # 脚本里 report_to="none" 未用，可删
